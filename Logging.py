# Logging
# The logging Module in Python is a Powerful builkt-in Modules
import logging

logging.info(f"this is a basics level of Logging")
logging.warning(f"This is a warning Message")
logging.critical(f"this is a critical message")
